<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogs</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="bg-zinc-900 text-white p-10">
        <a href="/" class="text-blue-500">back to create blog</a>
        <a href="/logout" class="text-blue-500 ml-10">Logout</a>
        <h1 class="mt-10 text-3xl">All Blogs</h1>
        <div class="w-full min-h-screen bg-zinc-900 text-white p-10 flex flex-wrap gap-4">
            <% if (blogs.length > 0) { %>
                <% blogs.forEach(blog => { %>
                <div class="card w-96 overflow-hidden h-96 bg-zinc-700 rounded-lg flex flex-col p-4">
                    <h1 class="text-2xl mb-2"><%= blog.title %></h1>
                    <p class="w-full h-full bg-zinc-500 rounded-lg p-2 overflow-y-auto"><%= blog.content %></p>
                    <div class="w-full flex justify-between pt-5">
                        <a href="/update/<%= blog._id %>" class="text-blue-500">Edit</a>
                        <a href="/delete/<%= blog._id %>" class="text-red-500">Delete</a>
                    </div>
                </div>
            <% }) %>
           <% } else { %>
                <h1 class="text-xl text-zinc-500">No Blogs Found</h1>
            <% } %>
            
        </div>
    </div>  
</body>
</html>
