<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <a href="/read" class="text-blue-500">explore blogs</a>
        <div class="blog-form mt-10 w-full h-full">
            <form action="/update/<%= blog._id %>" method="post" class="flex flex-col gap-4">
                <input type="text" value="<%= blog.title %>" name="title" placeholder="Title" class="bg-transparent rounded-lg outline-none max-w-72 py-2 px-4 border-2 border-zinc-500 ">
                <textarea class="bg-transparent max-w-96 rounded-lg outline-none py-2 px-4 border-2 border-zinc-500 " name="content" placeholder="Blog Description..."><%= blog.content %></textarea>
                <input type="submit" value="Update Blog" class="border-2 max-w-44 text-xl border-zinc-500 px-3 py-2 rounded-lg bg-blue-700 cursor-pointer">
            </form>
        </div>
    </div>
</body>
</html>